# Sample .env for Plant Classification App
# Copy to .env and edit values as needed

# Database connection string (PostgreSQL example)
SQLALCHEMY_DATABASE_URL=postgresql://<user>:<password>@localhost/<database_name>

# SQLite example (uncomment to use)
# SQLALCHEMY_DATABASE_URL=sqlite:///./plants.db